# 2026-08-11 工作日志

- 四象限任务管理：准备云服务器部署。给 server.js 增加可选 Token 鉴权（环境变量 TASK_TOKEN，
  未设置则内网/测试模式开放）并把监听地址从 127.0.0.1 改为 0.0.0.0（允许外部访问）。
  app.js 同步层增加 token 透传（从 URL ?token= 或 localStorage 读取，自动附加到 api/ping、api/state）。
- 新增 ecosystem.config.js（pm2）+ setup.sh 一键启动/保活。
- 用户路线：云服务器部署（轻量应用服务器，Ubuntu）。用户自述部署/运维偏小白，已多次用通俗类比讲解。
